---
name: deliver
description: Use when an internal spec needs to become a handoff PRD or a test strategy.
argument-hint: [feature-name]
---

# /deliver

1. Read `docs/workflow.md`.
2. Start from `templates/external-prd.md` and `templates/test-strategy.md`.
3. Save outputs to `delivery/`.
4. Use requirement IDs only when the document is meant for external handoff.
5. Check `docs/delivery-checklist.md` before calling the deliverable complete.
